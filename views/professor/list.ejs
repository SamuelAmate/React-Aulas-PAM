<!-- Cabeçalho da página: título à esquerda + botão "Novo professor" à direita -->
<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h4 mb-0">professor</h1>
  <!-- Link para o formulário de criação -->
  <a href="/professor/create" class="btn btn-primary">Novo Professor</a>
</div>

<!-- Card Bootstrap para dar um visual "caixa" -->
<div class="card shadow-sm">
  <div class="card-body p-0">
    <!-- Tabela responsiva para exibir as categorias -->
    <table class="table table-hover align-middle mb-0">
      <!-- Cabeçalho da tabela -->
      <thead class="table-light">
        <tr>
          <th>Nome</th>
          <th>Curso</th>
          <th class="text-end">Ações</th>
        </tr>
      </thead>
      <tbody>
        <% if (typeof professor !== 'undefined' && professor.length > 0) { %>
          <% professor.forEach(professor => { %>
            <tr>
              <td><%= professor.nome %></td>
              <td><%= professor.curso %></td>
              <td class="text-end">
                <a href="/professor/<%= professor.id %>/edit">
                  <button class="btn btn-warning btn-sm">Editar</button>
                </a>
              </td>
            </tr>
          <% }) %>
        <% } else { %>
          <tr>
            <td colspan="2" class="text-center py-3 text-muted">
              Nenhum professor cadastrado
            </td>
          </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</div>